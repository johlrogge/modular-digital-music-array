[package]
name = "acid-protocol"
version.workspace = true
edition.workspace = true

[package.metadata]

[package.metadata.polylith]
interface = "acid-protocol"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
