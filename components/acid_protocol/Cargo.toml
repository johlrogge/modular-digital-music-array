[package]
name = "acid-protocol"
version = "0.10.1"
edition = "2021"

[package.metadata]

[package.metadata.polylith]
interface = "acid-protocol"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
pretty_assertions = "1"
