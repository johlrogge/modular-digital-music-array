[package]
name = "admin-ipc-protocol"
version = "0.21.0"
edition = "2021"

[package.metadata]

[package.metadata.polylith]
interface = "admin-ipc-protocol"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
pretty_assertions = "1"
