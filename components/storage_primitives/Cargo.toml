[package]
name = "storage_primitives"
version = "0.2.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
pretty_assertions = { workspace = true }
