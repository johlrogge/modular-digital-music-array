[package]
name = "library-ipc-client"
version = "0.10.1"
edition = "2021"
authors = ["MDMA Development Team"]
repository = "https://github.com/your-org/mdma"
license = "MIT"

[package.metadata]

[package.metadata.polylith]
interface = "library-ipc-client"

[dependencies]
library-ipc-protocol = { workspace = true }
nng-transport = { workspace = true }
nng = "1.0"
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
